#Write a Python function that takes a string as input and returns the total number of characters in the string. The function should count all characters, including spaces, punctuation, and special characters.
def count_characters(str):
    # Code Starts

    # Write your code here
    return len(str)
    # Code Ends

# Predefined input
text = "Hello World"

# Function call
print(count_characters(text))